# OS
.DS_Store
Thumbs.db

# 開發用暫時預覽圖（非遊戲資源）
layout_actual.png
layout_preview.png
preview_v2.png

# README 用到的預覽圖：必須追蹤
# preview.jpg：在下方明確不 ignore（沒列就會被追蹤，所以不用 !）
# images/sample.png：在「牌圖素材」段用 images/* + ! exception 救回來

# 開發用暫時預覽頁面
layout-preview.html
sprite-preview.html
flower-preview.html

# 編輯器
.vscode/
.idea/

# 牌圖素材（已內嵌進 mahjong-solo.html，不對外公開）
# 用 images/* 而非 images/，這樣 README 用的 sample.png 才能用 ! exception 救回來
images/*
!images/sample.png
tiles-sprite.css

# AI 開發指引（本地用，不進 repo）
CLAUDE.md

# Node（如果未來加入）
node_modules/

# Skill 打包產物（由 .claude/ 生成，可隨時重建，不進 repo）
*.skill

# zip 操作失敗產生的暫存垃圾（隨機檔名，無副檔名）
ziChN8IB
zikIe0bP


# 3D 版備份（不進 repo）
index-3d.bak.html
# mahjong-3d-demo.html 在 README-3D.md 有引用，必須追蹤

# 排版檢查工具（本地除錯用）
final_runs/
layout_check_console.js
layout_check_playwright.py

# 設計素材壓縮包（不進 repo）
Mahjong Design System.zip

# 舊版備份檔
mahjong-solo.bak.html

# 桌面圖預覽（已內嵌進 html）
flat_table.webp
flat_table_preview.png

# 開發階段預覽圖 / HTML（preview.jpg 已在頂部 exception 開放）
preview.png
dice-animation.html
discard-preview.html
meld-corner-preview.html
reform-preview.html
square-layout-preview.html
square-preview.html
